<div class="scheduled-notification-row" @onclick="@OnRowClicked">
    <p>
        @Notification.Type
    </p>
    <p>
        @Notification.IntervalValue @Notification.IntervalType
    </p>
    <p>
        @Notification.StartAtLocal.ToString("HH:mm")
    </p>
</div>

@code {
    [Parameter] required public ScheduledNotificationDto Notification { get; set; }
    [Parameter] public EventCallback<ScheduledNotificationDto> NotificationClicked { get; set; }

    private async Task OnRowClicked()
    {
        await NotificationClicked.InvokeAsync(Notification);
    }
}
