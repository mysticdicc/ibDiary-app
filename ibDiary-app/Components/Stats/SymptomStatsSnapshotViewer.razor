<div class="symptom-stats-viewer stats-viewer">
    <header>
        <h4>@_stats.Symptom.Title</h4>
    </header>
    <div>
        <IbdIntInput Value="@_stats.TotalReportsCount" ValueExpression="@(() => _stats.TotalReportsCount)" Title="Total Reports" ReadOnly="true"/>
        <IbdIntInput Value="@_stats.MonthlyReportsCount" ValueExpression="@(() => _stats.MonthlyReportsCount)" Title="Monthly Reports" ReadOnly="true" />
        <IbdIntInput Value="@_stats.TotalStateChanges" ValueExpression="@(() => _stats.TotalStateChanges)" Title="Total State Changes" ReadOnly="true" />
        <IbdIntInput Value="@_stats.MonthlyStateChanges" ValueExpression="@(() => _stats.MonthlyStateChanges)" Title="Monthly State Changes" ReadOnly="true" />
    </div>
    <div>
        <div class="chart-wrapper">
            <ApexChart TItem="SymptomSeverityTrendPoint" Title="Severity Trend" Options="@_chartOptions">
                <ApexPointSeries TItem="SymptomSeverityTrendPoint"
                                 Items="@_stats.MonthlySeverityTrend"
                                 Name="Severity Trend"
                                 XValue="e => e.Date"
                                 YValue="e => Math.Round((decimal)e.AverageSeverity, 2)"
                                 SeriesType="SeriesType.Line" />
            </ApexChart>
        </div>
    </div>
</div>

@code {
    [Parameter] public SymptomStatsSnapshot Stats { get; set; } = new();
    private SymptomStatsSnapshot _stats = new();
    private readonly ApexChartOptions<SymptomSeverityTrendPoint> _chartOptions = new()
    {
        Chart = new Chart
        {
            Height = "200",
            ParentHeightOffset = 0
        },
        Legend = new Legend { Show = false },
        DataLabels = new DataLabels { Enabled = true },
        Stroke = new Stroke { Width = 3, Curve = Curve.Smooth },
        Xaxis = new XAxis
        {
            TickAmount = 8,
            Labels = new XAxisLabels
            {
                Rotate = -90,
                RotateAlways = true,
                HideOverlappingLabels = false,
                Trim = false,
                MinHeight = 70,
                MaxHeight = 70
            }
        },

        Grid = new Grid
        {
            Padding = new Padding
            {
                Top = 0,
                Right = 10,
                Bottom = 0,
                Left = 0
            }
        }
    };

    protected override void OnParametersSet()
    {
        if (Stats != _stats) _stats = Stats;
        base.OnParametersSet();
    }
}
